; FM Analyser example configuration file
; Generated by inigen.py 0.1.dev, 2012-05-23 22:31:05.320942

; Section: global
; Software global parameters.
; These can be overriden by command line options.
[global]
; Logging level as defined by the python logging module.
; (DEBUG=10, INFO=20, WARNING=30, CRITICAL=50).
; Default: 30.
loglevel = 30

; data_dir. Default: /home/gabriel/.fmanalyser/var.
data_dir = /home/gabriel/.fmanalyser/var

; watcher_sleep_time. Default: 0.1.
watcher_sleep_time = 0.1

; empty_queue_timeout. Default: 1.
empty_queue_timeout = 1


; Section: device
[device]
; enabled. Default: False.
enabled = False

; scan_lock_time. Default: 1.
scan_lock_time = 1


; Section: device:rtl_example
[device:rtl_example]
; enabled. Default: False.
enabled = False

; model. Default: p175.
model = p175

; scan_lock_time. Default: 1.
scan_lock_time = 1

; fft_size. Default: 1024.
fft_size = 1024

; sample_count. Default: 262144.
sample_count = 262144


; Section: device:grrtl_example
[device:grrtl_example]
; enabled. Default: False.
enabled = False

; model. Default: p175.
model = p175

; scan_lock_time. Default: 1.
scan_lock_time = 1


; Section: device:funcube_example
[device:funcube_example]
; enabled. Default: False.
enabled = False

; model. Default: p175.
model = p175

; scan_lock_time. Default: 1.
scan_lock_time = 1

; Number of signal strength measurement to perform per frequency while scanning
; Default: 10.
scan_samples = 10

; Delay (s) between two samples
; Default: 0.1.
scan_sample_delay = 0.1


; Section: device:p175_example
[device:p175_example]
; enabled. Default: False.
enabled = False

; model. Default: p175.
model = p175

; scan_lock_time. Default: 1.
scan_lock_time = 1

; Time during the program sleeps, waiting for the device to acquire measurements in rds mode.
; Default: 5.
rds_lock_time = 5

; Time during the program sleeps, waiting for the device to acquire measurements in measuring mode.
; Default: 5.
mes_lock_time = 5

; Time during the program sleeps, waiting for the device to acquire measurements in stereo mode.
; Default: 5.
stereo_lock_time = 5


; Section: snmpd
[snmpd]
; ipv4. Default: True.
ipv4 = True

; host. Default: 127.0.0.1.
host = 127.0.0.1

; ipv6. Default: False.
ipv6 = False

; ipv6_host. Default: ::1.
ipv6_host = ::1

; port. Default: 161.
port = 161

; v1. Default: True.
v1 = True

; v2c. Default: True.
v2c = True

; security_name. Default: security.
security_name = security

; community_name. Default: public.
community_name = public

; v3. Default: True.
v3 = True

; auth_protocol. Default: MD5.
; Choices: [SHA1, MD5]
auth_protocol = MD5

; priv_protocol. Default: DES.
; Choices: [AES, DES]
priv_protocol = DES

; user_name. Default: user.
user_name = user

; user_security_level. Default: noAuthNoPriv.
; Choices: [noAuthNoPriv, authNoPriv, authPriv]
user_security_level = noAuthNoPriv

; user_auth_key. Default: auth_key.
user_auth_key = auth_key

; user_priv_key. Default: priv_key.
user_priv_key = priv_key

; admin_name. Default: admin.
admin_name = admin

; admin_security_level. Default: authNoPriv.
; Choices: [noAuthNoPriv, authNoPriv, authPriv]
admin_security_level = authNoPriv

; admin_auth_key. Default: auth_key.
admin_auth_key = auth_key

; admin_priv_key. Default: priv_key.
admin_priv_key = priv_key


; Section: redis
[redis]
; host. Default: localhost.
host = localhost

; port. Default: 6379.
port = 6379

; db. Default: 0.
db = 0

; expire. Default: 30.
expire = 30


; Section: plugin
; 
; Base config for all user defined plugins.
; It must be extended for each plugin by creating a section named [plugin:<your_plugin_name>].
[plugin]
; Full python class name (e.g. myapp.mymodule.MyPluginClass)
; Required.
cls = None


; Section: test
[test]
; live_tests. Default: True.
live_tests = True


